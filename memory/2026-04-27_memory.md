## MEMORY.md

Hermes skills 仓库: https://hermes-agent.nousresearch.com/docs/skills
§
创建 cron 任务时必须走完整的 "create → 验证 → update repeat" 两步流程：先用 repeat=1 创建确保成功，再用 update 升级为 repeat=9999。严禁直接用 repeat="forever" 创建（会触发 cron 系统类型比较 bug）。
§
浏览器任务分层策略：简单任务用内置 browser_* 工具（直接连接已有 Chrome）；复杂自动化任务（如需 CDP 精细控制、自愈机制）用 browser-harness 技能。这样避免不必要的 Chrome 进程开销。
§
sing-box 配置 key: 字段名用 `route.rules`（不是 `routing.rules`），rule type 用 `default`（不是 `field`），TLS/reality 放在 `outbounds[].tls.reality`（不是顶层）。reality public_key 来源于订阅链接中 vless URL 的 `pbk=` 参数。
§
代理访问策略：被墙网站用 curl --proxy socks5h://127.0.0.1:1080，不走浏览器代理（避免 Chrome 进程开销）。能直接访问的（Wikipedia/GitHub/Google等）直接 curl，无需代理。
§
问题解决策略：难题先试2种方法，不超过3次。超过后主动询问用户，不要独自继续尝试。遇到不确定的弯路立即停下来问用户意见。
§
deliver="origin" 的 cron 任务，脚本内部不要调 Feishu API 发消息——cron 系统会把 stdout 自动推送给用户，脚本自己也调 API 会导致用户收到两条重复消息。正确：脚本只输出到 stdout。
§
Supabase 项目 hjejbcfrpnzslfoptfdu 凭证（可读写 DB）：
- URL: https://hjejbcfrpnzslfoptfdu.supabase.co
- Anon Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
- Service Role Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
- Management Token: sbp_c72a3eb0294333d6be77565e27c00f27e79997c8
- cities 表已创建，112个城市数据已导入


## USER.md

姓名: Medivh
公司: HSBC China
语言: 中文 & English
职位: Database Administrator
技术栈: MongoDB, Oracle, MySQL, PostgreSQL
§
Trello: 用户名 MedivhChen, 看板 HSBC_TASK, 看板ID cXJqIcUp, 已连接
§
不要推送系统操作通知（cron创建、技能更新、memory更新等），只推送用户实际需要看到的内容
§
不要推送系统操作通知（cron创建/完成、技能更新、memory更新等），只推送用户需要看到的内容
§
回复格式偏好：使用emoji + 分隔线 + 分级标题展示任务列表，定时任务用表格+状态图标，Trello任务按紧急程度分组，底部加分隔线。具体风格见今日格式示例。
